# bili娘站 · 信息修改说明书

本说明书适用于**在本地 PyCharm 修改网站内容后推送到线上**。涵盖三类修改：
**① 哔哩娘数据**　**② 设定（角色介绍 / 站点信息）**　**③ 图片**

---

## 〇、文件分工速查

| 文件 | 作用 |
|---|---|
| `bili_data_a.py` | 哔哩娘数据（第 1 部分）：No.01、No.02、No.06、No.18、No.73 |
| `bili_data_b.py` | 哔哩娘数据（第 2 部分）：No.33、No.22 |
| `wz.py` | 主入口：页面配置、顶部搜索、拼装数据、渲染 |
| `ui.py` | 卡片/页脚渲染、图片与文字解析函数 |
| `bili_dialog.py` | 点击「查看详情」弹出的详情弹窗 |
| `bili_banner.py` | 顶部活动提示栏（默认关闭） |
| `bili_style.py` | 全部 CSS 样式（卡片、弹窗、配色、隐藏外壳） |
| `.streamlit/config.toml` | **强制浅色主题**等站点级配置 |
| `22图.jpg`、`忆人摘星头像.png` | 图片文件，放在**仓库根目录** |

> 数据分 a/b 两个文件只是拆分化，线上会合并展示（a + b，再按 No.01~No.73 排序）。
> 新增/修改某个哔哩娘时，编进 a 或 b 都行，只要 `id` 不重复。

---

## 一、首次环境准备（只做一次）（已做）

在 PyCharm 的 Terminal（终端）里：

```bash
# 1. 若 clone 时报 "Error in the HTTP2 framing layer"，先执行下面这行再 clone：
git config --global http.version HTTP/1.1

# 2. 克隆仓库（已克隆可跳过）
git clone https://github.com/loveforlove756-commits/bili-girls-site.git

# 3. PyCharm 打开该文件夹后，安装依赖
pip install -r requirements.txt

# 4. 本地预览（必须在仓库根目录运行）
streamlit run wz.py
# 浏览器打开 http://localhost:8501 ，改代码保存会自动刷新
```

> `push` 需要登录 GitHub：弹窗授权最方便；若用账号密码，密码处填 **Personal Access Token**（不是 GitHub 登录密码）。

---

## 二、修改哔哩娘数据（① 数据）

打开 `bili_data_a.py` 或 `bili_data_b.py`，找到 `bili_girls_part = [...]` 列表，
每个 `{ ... }` 就是一个哔哩娘。字段含义：

| 字段        | 必填 | 说明 |
|---         |---   |---|
| `id`       |  ✅  | 编号，格式 `No.01` ~ `No.73`（两位补零） |
| `name`     |  ✅  | 显示名字，如 `"22"`、`"33"`；没有就写 `"暂无"` |
| `img`      |   X  | 主卡片封面图文件名，如 `"22图.jpg"`；没有就写 `""` |
| `votes`    |  ✅  | 得票数，**用字符串**，如 `"1824"`、`"暂未搜集"` |
| `desc`     |  ✅  | 详细文字介绍（含「设定」），用三引号 `"""..."""` 包多行 |
| `designer` |   X  | 设计者（可选，部分角色有） |
| `gallery`  |   X  ｜ 详情弹窗里的图片列表，见下方说明 |

### 示例：新增一个完整角色（No.74 不存在，需手动加；No.01~73 已自动占位）

```python
{
    "id": "No.74",
    "name": "示例娘",
    "img": "no74.jpg",
    "votes": "0",
    "desc": """设定：
身高：160cm
性格：示例
爱好：ACG""",
    "gallery": [
        {"url": "no74.jpg", "title": "经典主视觉"},
        {"url": "https://example.com/pic.png", "title": "网络图（直接填链接也可）"}
    ]
},
```

### 要点
- **不用手动补齐 73 个**：`wz.py` 会自动把 No.01~No.73 里没写到的编号用「暂无」占位。你只需维护手头真正有的角色。
- `gallery` 里每项 `{"url":..., "title":...}`：`url` 可填**仓库根目录的图片文件名**，也可直接填 `https://` 网络链接。
- 修改已有角色：直接改对应字典里的字段值即可。

---

## 三、修改设定（② 设定）

### 3.1 单个角色的「设定」文字
就是数据里的 `desc` 字段（见上）。直接编辑即可，支持多行。

### 3.2 站点级信息（页脚作者）
在 `ui.py` 的 `render_footer()` 里：
```python
MY_NAME = "忆人摘星"                                  # 作者名
MY_SPACE_URL = "https://space.bilibili.com/3546929877224366"  # 作者空间
MY_AVATAR_PATH = "忆人摘星头像.png"                   # 作者头像（仓库根目录图片）
```

### 3.3 顶部活动提示栏
在 `bili_banner.py` 顶部配置区：
```python
BANNER_MODE = "off"        # "off"关闭 | "on"常开 | "date"指定日期 | "datetime"时间段
TARGET_DATE = (8, 16)      # 月,日（配合 date 模式）
START_TIME / END_TIME      # 配合 datetime 模式
BANNER_TEXT = "..."        # 栏内文字
BANNER_IMG = ""            # 栏内小图（仓库图片名或链接）
BANNER_LINK = "..."        # 点击跳转链接
BANNER_BG_COLOR / BANNER_TEXT_COLOR / BANNER_BORDER_COLOR  # 配色
```

### 3.4 站点主题配色（强制浅色）
在 `.streamlit/config.toml`：
```toml
[theme]
base = "light"                 # 强制浅色，勿改
primaryColor = "#00A1D6"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F4F5F7"
textColor = "#18191C"
```
> 弹窗/卡片的视觉样式在 `bili_style.py`，非必要不要动。

---

## 四、修改 / 添加图片（③ 图片）

1. **放位置**：图片文件直接放进**仓库根目录**（和 `wz.py` 同级），不要建子文件夹。
2. **命名**：建议英文+数字，如 `no74.jpg`（中文名如 `22图.jpg` 能用，但跨平台偶有编码坑）。
3. **引用**：在数据里把 `img` 或 `gallery[].url` 填成该文件名（带扩展名），例如 `img:"no74.jpg"`。
4. **本地预览**：必须在仓库根目录运行 `streamlit run wz.py`，相对路径才找得到图。
5. **上线必须**：图片是二进制文件，**一定要 `git add` + `commit` + `push`**，否则线上显示「图片资料暂未搜集」。

> 代码另有 `.b64` 后备方案（把图转 base64 存文本），一般无需使用；直接放 jpg/png 最简单。

---

## 五、提交并上线

改完所有内容后，在 **PyCharm 底部的「Terminal / 终端」**（就是跑过 `git clone`、`streamlit run wz.py` 同一个面板）里执行：

> 💡 **重点澄清（最容易混淆的两点）**
> - **上线不用在 git 里写图片文件名。** 图片名你只在数据文件里写一次（`img:"no74.jpg"`），那是给网站看的，告诉它显示哪张图。git 那一步完全不用写文件名。
> - **图片是怎么被带上线的？** 因为图片文件已被你拖进 `bili-girls-site` 文件夹，`git add .` 里的「点 `.`」=「把当前文件夹里所有改动和新文件都加进来」，图片作为文件夹里的新文件会被**自动一起带上**，你不用单独指定它。

```bash
git add .                                    # 「.」= 当前文件夹全部改动，图片和 .py 一起加入
git commit -m "加 No.74 + 改 22娘设定"        # 写清楚改了啥
git push                                     # 推送，Render 自动重建部署
```

**提交前想确认图片真的会被带上？** 在 `git commit` 之前先跑：
```bash
git status
```
列表里会出现 `new file:   no74.jpg`、`modified:   bili_data_a.py` 之类。只要图片文件名出现在里面，就说明它会被一起推送。确认无误再 `commit` + `push`。

推送后等 1~2 分钟，**硬刷新**浏览器（Cmd/Ctrl+Shift+R）即可看到更新。

> PyCharm 图形化方式：顶部 Git 工具条 `✓ Commit` → 填说明 → `Commit & Push`。

---

## 六、常见问题

**Q1. clone / push 报 `Error in the HTTP2 framing layer`**
先执行 `git config --global http.version HTTP/1.1` 再重试；若仍不行，关掉 VPN/代理重试，或改用 SSH 地址 `git@github.com:loveforlove756-commits/bili-girls-site.git`。

**Q2. 图片在线上不显示，本地正常**
多半是**图片没一起 push**（只改了 .py）。确认 `git add .` 包含了图片文件并重新 push。另检查 `img` 文件名与实际文件是否完全一致（大小写、扩展名）。

**Q3. 本地图片显示「图片资料暂未搜集」**
没在仓库根目录运行 `streamlit run wz.py`，或 `img` 填的文件名和实际不符。

**Q4. push 要输密码，但密码不对**
GitHub 已不支持账号密码推送，密码处填 **Personal Access Token**（在 GitHub → Settings → Developer settings 生成，需勾选 `repo` 权限）。

**Q5. 想加第 74 号以后的角色**
`wz.py` 只自动占位到 No.73。No.74+ 需在 `bili_data_a.py` / `bili_data_b.py` 里**手动加条目**（参考第二节示例），并注意 `id` 不要和已有重复。
